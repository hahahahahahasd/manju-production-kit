# 提取范围

来源：`manju-projects/仙门速成班`。保留原项目和全部素材，不移动、不删除。

复用：`manju-production` 的导演/审片规则、镜头卡校验器、ComfyUI/Wan 适配器、1–3 镜批次管理器、监督器和 MCP 入口。仓库保留原相对目录结构，因此脚本无需依赖旧项目的绝对路径。

改动：去掉技能说明中的 E01 专属人物和开场案例；去掉写死 45 镜的进度脚本依赖；监督器默认关闭；适配器的离线校验增加工作流绑定和源图哈希检查。

未移入：所有 E01 配置/批次/运行记录/音视频/角色场景图、真实 ComfyUI 工作流与 GPU 连接信息。原项目当前的审批状态不会被带入新项目。
